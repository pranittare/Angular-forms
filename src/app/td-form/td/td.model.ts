export class TD {
  constructor(public fullname: string, public add1: string, public add2: string, public zip: string, public state: string){
    this.fullname = fullname;
    this.add1 = add1;
    this.add2 = add2;
    this.zip = zip;
    this.state = state;
  }
  // fullname: string, 
  // add1: string, 
  // add2: string, 
  // zip: string, 
  // state: string
}